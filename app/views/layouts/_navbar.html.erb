<nav class="bg-transparent border-gray-200 p-5 sm:p-6 rounded dark:bg-gray-900">
  <div class="container flex flex-wrap justify-between items-center mx-auto">

    <%= link_to root_path, class: 'flex items-center hover:no-underline' do %>
      <span class="self-center text-xl font-semibold whitespace-nowrap dark:text-white">Csvs</span>
    <% end %>

      <% if current_user %>
        <ul class="flex bg-transparent">
          <li>
            <%= link_to 'Dasboard', csvs_path, class: 'btn btn-ghost' %>
          </li>
          <li>
            <%= button_to 'Sign out', logout_path, method: :delete, class: 'btn btn-ghost' %>
          </li>
        </ul>

      <% else %>
        <ul class="flex bg-transparent">
          <li>
            <%= link_to 'Sign in', new_session_path, class: 'btn btn-ghost' %>
          </li>
          <li>
            <%= link_to 'Sign up', new_user_path, class: 'btn' %>
          </li>
        </ul>
      <% end %>
  </div>
</nav>


